# public/

Carpeta raíz del servidor web. Es la única carpeta expuesta al navegador.

Contiene el punto de entrada (index.php), el archivo de rutas del servidor (.htaccess)
y los recursos estáticos (CSS, JS, imágenes).
